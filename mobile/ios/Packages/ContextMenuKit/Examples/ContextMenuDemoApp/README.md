# ContextMenuDemoApp

This example app demonstrates `ContextMenuKit` as a local Swift package dependency.

Open:

- `ContextMenuDemoApp.xcodeproj`

The project is checked in directly and does not require `xcodegen` or other project-generation tools.
