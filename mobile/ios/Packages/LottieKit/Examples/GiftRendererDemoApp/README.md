# GiftRendererDemoApp

This example app demonstrates `LottieKit` as a local Swift package dependency.

Open:

- `GiftRendererDemoApp.xcodeproj`

The project is checked in directly and does not require Bazel, `xcodegen`, or other project-generation tools.
